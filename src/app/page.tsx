'use client'
import React from 'react'

const page = () => {
  return (
    <main>
    <div>page</div>
    <button onClick={()=>alert("hello next js")}>Click me</button>
    </main>
  )
}

export default page
